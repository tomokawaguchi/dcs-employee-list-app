import { describe, it, expect } from "vitest";

// Checking to see if the test runs
describe("Setup tests", () => {
	it("true test", () => {
		expect(true).toBe(true);
	});

	it("False test", () => {
		expect(false).toBe(false);
	});
});
